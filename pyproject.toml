[project]
name = "kleinanzeigenbot"
version = "0.1.0"
description = "Kleinanzeigen bot project"
requires-python = "==3.13.*"   # or the version you actually use (e.g. >=3.11)
dependencies = [
    "beautifulsoup4==4.14.3",
    "black==26.3.1",
    "bs4==0.0.2",
    "isort==8.0.1",
    "pre-commit==4.6.0",
    "python-dotenv==1.2.2",
    "python-telegram-bot[job-queue]==22.7",
    "requests==2.33.1",
]



[tool.black]
line-length = 88
target-version = ["py313"]
include = '\.pyi?$'
exclude = '''
/(
    \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.nox
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
)/
'''
